# Taswouk API base URL (no trailing slash). Defaults to https://v2.taswouk.com in code if unset.
VITE_API_BASE_URL=https://v2.taswouk.com

# Authorization scheme for API requests (default Bearer). Try JWT if profile returns 401 with a valid login.
# VITE_AUTH_SCHEME=JWT
